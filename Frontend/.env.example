# Base URL of the PMAP backend API.
# Copy this file to .env and adjust values for your environment.
# In Docker, this value is injected at build time as the VITE_API_URL build arg
# (see docker-compose.yml and .github/workflows/docker-images.yml).
VITE_API_URL=http://localhost:8080/api